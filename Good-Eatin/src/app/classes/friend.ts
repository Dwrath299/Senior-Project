
export class Friend {
    id: string;
    name: string;
    isContributor: boolean;

    constructor(id: string, name: string, isContributor: boolean) {
        this.id = id;
        this.name = id;
        this.isContributor = isContributor;
    }
}
